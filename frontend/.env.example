# Frontend environment variables (CRA only inlines variables prefixed with REACT_APP_)
# Copy to .env.local for local overrides; .env.production is the committed deployment value.

# Base URL of the backend API (no trailing slash).
REACT_APP_API_URL=http://localhost:5062
